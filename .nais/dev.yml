kind: Application
apiVersion: nais.io/v1alpha1
metadata:
  name: paw-brukerstotte-test
  namespace: paw
  labels:
    team: paw
spec:
  image: {{ image }}
  port: 8000
  ingresses:
    - https://paw-brukerstotte-test.ansatt.dev.nav.no
  liveness:
    path: /isalive
    initialDelay: 3
  readiness:
    path: /isready
    initialDelay: 3
  azure:
    application:
      enabled: true
    sidecar:
      enabled: true
  replicas:
    min: 1
    max: 1
  accessPolicy:
    outbound:
      rules:
        - application: paw-arbeidssoekerregisteret-hendelselogg-backup
        - application: paw-arbeidssoekerregisteret-api-oppslag-v2
  env:
    - name: HENDELSELOGG_BACKUP_URL
      value: http://paw-arbeidssoekerregisteret-hendelselogg-backup/api/v1/arbeidssoeker/detaljer
    - name: OPPSLAG_API_V2_URL
      value: http://paw-arbeidssoekerregisteret-api-oppslag-v2
